spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liontalk/SpringCloud_Config
          username: liontalk@163.com
          password: Liontalk@1234
          default-label: master
server:
  port: 8888